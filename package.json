{
  "name": "flower-shop",
  "version": "1.0.0",
  "description": "Flower Shop - Multi-service e-commerce platform",
  "private": true,
  "packageManager": "pnpm@8.0.0",
  "scripts": {
    "install-all": "pnpm install",
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  }
}
